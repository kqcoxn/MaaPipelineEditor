{
  "name": "maa-pipeline-editor-desktop",
  "private": true,
  "version": "2.0.0",
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:editor": "yarn --cwd ../Editor build --mode desktop",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
