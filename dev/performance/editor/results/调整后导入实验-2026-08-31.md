# PERF-001 调整后数据集导入实验

## 结论

状态：导入链路通过；扩展浏览器指标未取得

三档调整后数据集均通过真实 `pipelineToFlow` 导入。测试覆盖 JSON 解析、配置解析、节点和边创建、Group 父子关系及坐标归一化、历史初始化、FlowStore 替换和 Anchor 引用索引重建。

三档 `LayoutHelper.auto()` 调用次数均为 0，确认带完整坐标的数据集不会触发自动排版。首个 Group 子节点导入后的绝对坐标与文件中的 `$__mpe_code.position` 完全一致。

## 单次实验结果

命令：`yarn --cwd Editor vitest run src/core/parser/performanceDataset.test.ts --reporter=verbose`

| 数据集 | 节点 | 边 | 导入耗时 | 自动排版调用 | 数量与坐标 |
| --- | ---: | ---: | ---: | ---: | --- |
| small | 100 | 200 | 6.49 ms | 0 | 通过 |
| medium | 200 | 500 | 5.81 ms | 0 | 通过 |
| large | 300 | 900 | 9.10 ms | 0 | 通过 |

耗时为本次 Vitest/happy-dom 进程中的单次观测，只用于确认导入算法没有出现数量级异常，不能替代浏览器 Scripting、Rendering、Painting、Long Task、Heap、DOM 或交互流畅度指标。

## 全量回归

调整完成后再次执行 `yarn --cwd Editor vitest run`，共 76 个测试文件、301 项测试全部通过。三档数据在全量测试进程中的导入耗时分别为 9.42 ms、24.54 ms 和 18.00 ms，自动排版调用次数仍全部为 0。

同时通过以下检查：

- `yarn perf:verify`
- `yarn lint`
- `git diff --check`

## 浏览器实验状态

本次尝试启动生产预览并重新连接内置浏览器，但旧 1000 节点测试留下的浏览器渲染会话仍未恢复。新建空页面后，Playwright DOM 快照和可见 DOM 两种接口均在 20 至 40 秒内超时，因此没有继续导入新数据集，避免把环境阻塞错误归入 300 节点结果。

后续优化需要浏览器渲染指标时，应在一个已恢复或独立的浏览器进程中按 100、200、300 顺序执行；该扩展测量不阻塞 PERF-001 完成。
