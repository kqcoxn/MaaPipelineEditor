- 采用内容集中化管理模式，将页面所需的文案、链接和配置项统一定义在 `src/content/landing.ts` 中，页面组件仅负责引用和渲染。
- 交互组件遵循渐进式增强原则，仅在必要时（如复杂的状态管理或动画）使用 React 组件并通过 `client:idle` 指令加载，其余部分保持纯 HTML/CSS。
- 布局组件 `LandingShell` 统一处理全站级别的 SEO 优化，包括动态生成 Open Graph 标签、Twitter Card 以及 Schema.org 结构化数据。