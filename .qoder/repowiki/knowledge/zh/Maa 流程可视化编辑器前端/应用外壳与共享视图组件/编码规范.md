- 采用 React.lazy 配合 Suspense 对非首屏关键组件（如 JsonViewer、DebugModal）进行代码分割与懒加载，以优化初始渲染性能。
- 在 useEffect 中通过注册回调函数（如 localServer.onStatus）订阅全局服务状态变化，并在清理函数中取消订阅以防止内存泄漏。
- 使用自定义 Hook（如 useEmbedMode、usePanelOccupancy）封装复杂的环境检测与面板显隐逻辑，保持主组件函数的简洁性。