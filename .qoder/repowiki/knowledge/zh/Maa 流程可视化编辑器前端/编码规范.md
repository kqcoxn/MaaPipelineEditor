- 采用 Protocol 模式组织 WebSocket 通信逻辑，每个业务域对应独立的 Protocol 类并统一在 `initializeWebSocket` 中注册。
- 使用 CSS Modules (`.module.less`) 管理组件样式，确保样式隔离并遵循 `kebab-case` 命名规范。
- 复杂面板组件采用 `lazy` 懒加载策略，配合 `Suspense` 优化首屏渲染性能。