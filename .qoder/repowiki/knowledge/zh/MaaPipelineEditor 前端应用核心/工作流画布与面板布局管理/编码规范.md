- 采用“监控器组件”模式（如 `InstanceMonitor`），在 React Flow 内部通过副作用同步外部 Store 状态，保持主组件纯净。
- 面板互斥逻辑采用“声明式注册 + Store 驱动”架构，面板通过 `registerPanel` 预定义区域与反应形态，运行时由 Store 统一调度。
- 复杂交互状态（如连接起始点、面板抑制点击）优先使用 `useRef` 存储瞬态标志位，避免不必要的组件重渲染。