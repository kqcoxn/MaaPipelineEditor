---
trigger: always_on
---

## 本地文档路径
- MaaFramework 应用指南：`/instructions/maafw-guide/`（可用 `mfw` 或 `maafw` 指代）
- MaaFramework Go 绑定：`/instructions/maafw-golang-binding/`（localbridge 所需，所有 API 可参考`\instructions\maafw-golang-binding\API大全.md`）
- Ant Design：`/instructions/ant-design/`（可缩写为 `antd`）
- React Flow：`/instructions/react-flow/`
- Wails：`/instructions/wails/`



> 若需其他框架 API 且本地无文档，可联网检索。
>
> 文档内的内容不要修改，相当于只读。