---
trigger: always_on
---

## 本地文档路径（`/instructions/` 下）
- MaaFramework 应用指南：`maafw-guide/`（可用 `mfw` 或 `maafw` 指代）
- MaaFramework Go 绑定：`maafw-golang-binding/`（localbridge 所需）
- Ant Design：`ant-design/`（可缩写为 `antd`）
- React Flow：`react-flow/`
- Wails：`wails/`

> 若需其他框架 API 且本地无文档，可联网检索。