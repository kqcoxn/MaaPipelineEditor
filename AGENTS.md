# AGENTS.md

## 项目地址

- 名称：MaaPipelineEditor (mpe)
- 地址：https://github.com/kqcoxn/MaaPipelineEditor

## 项目结构

- **前端（editor）**：`/Editor`
- **后端（localbridge / lb）**：`/LocalBridge`
- **桌面端启动器（MPE Desktop）**：`/Desktop`
- **页面嵌入测试（iframe）**：`/Iframe`
- **文档站**：`/DocumentStation`
- **展示页/主页（landing）**：`/Landing`

> 默认开发需同时完善前后端，且在需要时更新文档站（注意是确定式更新，不要说在xxx版本如何或之前版本如何，文档是指导用户用的，不是记录某段代码是干啥的，因此我们不需要描述变更，只需要能做到在当前版本指导用户。此外，并非每次新东西都必须更新文档，仅在需要时更新即可）。
> 根目录 `package.json` 仅提供仓库级命令代理；Editor 依赖使用 `yarn editor:install` 安装。

## 开发准则

- 始终使用中文与我交流
- 使用 `yarn` 作为包管理器
- 新功能若与现有功能有关，应尽可能复用现有函数，在合适时可以进行方法封装或提升
- 前后端单个文件不应过大，超过时应该分模块封装（可以顺手拆分或重构）
- 不论前后端，实现不考虑旧版本兼容性，废弃内容直接移除（除非特别说明）
- 前端交互上的更新，不要自动执行 `yarn dev` 开启服务器或打开内嵌页面检查，也不要使用浏览器测试，我手动测试即可（在对话最后告诉我需要测哪些内容）
- 在需要参考相关社区项目时（即使用户没有提起进行参考，你也需要主动看看有没有可参考内容，以确保与社区主流做法接轨），可按 `dev\docs\社区参考项目索引.md` 推荐进行参考

## 测试准则

- 按实际回归风险写测试，不要求每次改动都补测试，不为覆盖率或数量凑用例。
- 普通文案、样式、静态配置、简单取值或转发不单独写测试；不重复已有覆盖，不锁死无关实现细节。
- 优先测试数据安全、核心行为、异常与竞态、协议边界及真实性能；避免只验证 mock 调用或在测试中重写实现、自算结果。
- 精简测试时保留有效行为断言，不统一弱化为“存在/非空”；修改后运行相关检查。
