---
title: 完整流程（视频教程）
permalink: /guide/start/video-tutorial
categories:
  - 指南
tags:
  - 指南
---

本页为视频教程的配套文档，方便跟练时查阅步骤、复制代码与链接。

::: tip
视频发布后链接将补充于此。
:::

## 概述

基于 MaaFramework 的自动化项目开发通常需要手写大量 JSON 配置文件，MaaPipelineEditor (MPE) 提供了一套可视化工具，可以直观地预览、构建、调试 MFW 项目。

MPE 与 IDE 的本质区别不在于开发方式的不同，而是**使用流程图可以在较小的视觉注意力范围内看到尽可能多的节点拓扑关系，信息密度大**，便于流程开发

本章将按照完整的开发流程，依次介绍环境准备、项目搭建、编辑器使用、调试方法及打包发布，大多数技能为 MFW 基础，仅约 1/3 为 MPE 教程。但我们仍然**推荐您首先从 [MFW 的官方文档](https://maafw.com/docs/1.1-QuickStarted)上手，尝试使用常规 IDE 制作可运行 demo 后再决定是否使用 MPE 作为主力编辑器**。

如果您已有充分的 MFW 基础，更推荐您阅读 [快速上手](/guide/start/quick-start) 章节起步。

## 准备工作

开始前需准备以下基础工具。

### Python

MaaFramework 的部分模块依赖 Python 环境运行。请访问 [Python 官网](https://www.python.org/) 下载并安装最新稳定版，安装时勾选 **Add Python to PATH**。

### VSCode

[VSCode](https://code.visualstudio.com/) 是主流的代码编辑器，拥有丰富的插件生态，推荐用于 MFW 项目开发。

### MaaFramework 官方支持插件

[插件](https://github.com/neko-para/maa-support-extension) 提供语法检查、智能提示、集成工具、运行调试等功能。

可直接在 VSCode 扩展面板中搜索 `Maa Pipeline Support` 安装。

## 配置本地 MFW 环境

环境准备完成后，需搭建 MFW 项目结构。

MaaFramework 提供了官方项目模板与脚手架 **[MaaPracticeBoilerplate](https://github.com/MaaXYZ/MaaPracticeBoilerplate)**：

将仓库 Clone 或下载到本地，即可获得标准的项目目录结构，包括资源文件夹、Pipeline 配置目录及构建脚本，详情请见仓库 README。

## MPE 基础使用

### 打开编辑器

MPE 提供两种使用方式：

- **在线版**：<https://mpe.codax.site/stable> — 无需安装，打开浏览器即可使用
- **本地一体包**：集成了所有依赖，长期使用更便捷，部署方式请参考 [本地一体包](/guide/extremer/deploy) 章节

### 界面布局

MPE 的核心设计理念是 **所见即所思**，界面由三个主要区域组成：

- 左侧 **工具栏**：快速添加节点、切换面板
- 中间 **画布**：Pipeline 逻辑的可视化呈现
- 右侧 **属性面板**：编辑选中节点的详细配置

### 节点类型

MPE 提供以下基本节点类型：

- **Pipeline 节点**：最常用的节点，包含识别和动作两部分。识别决定"看到什么"，动作决定"做什么"
- **External 节点**：引用外部 JSON 文件，适合跨文件复用逻辑
- **Anchor 节点**：锚点节点，用于优化连线布局，让复杂流程更清晰
- **Sticker 节点**：便签节点，用于添加注释说明，方便团队协作
- **Group 节点**：将相关节点分组，视觉上更整洁

### 添加与连接节点

从左侧节点库拖拽到画布，或使用快捷键创建节点。从一个节点的输出端口拖拽到另一个节点的输入端口即可建立连接。

### 编辑节点配置

选中节点后，右侧面板会显示所有可配置字段。MPE 为每种识别类型和动作类型提供了表单界面，填写表单后系统会自动生成符合规范的 JSON 配置，无需手写。

更详细的操作说明请参考 [快速上手](/guide/start/quick-start) 章节。

## 常用设置

建议在使用前根据需要调整以下配置，所有配置项集中在配置面板中管理。

### 文件配置

设置默认工作目录，建议指向 MFW 项目模板的 resource 目录，便于保存和加载。

### 本地服务配置

启用 LocalBridge 后，可设置服务地址和端口，默认为 `localhost` 加指定端口，一般无需修改。详细部署方式请参考 [本地服务](/guide/server/deploy) 章节。

### AI 配置（可选）

配置 API Key 和模型参数后，可使用 AI 辅助功能，包括智能节点搜索、配置自动补全、根据截图推断节点参数等。

### 外观设置

可切换明暗主题、调整画布缩放和网格显示等视觉参数。

## 本地服务

LocalBridge 是 MPE 的本地服务组件，通过一行命令即可安装，提供文件管理、截图工具、流程调试等增强功能。部署方式请参考 [本地服务部署](/guide/server/deploy) 章节。

启动后，MPE 通过 WebSocket 自动与 LocalBridge 通信，连接成功后状态栏会显示连接指示。

启用本地服务后可使用以下能力：

- 在编辑器内**浏览和管理项目文件**，无需切换窗口
- 使用**截图工具**实时捕获设备画面，进行文本识别、取色框选等操作，识别结果可直接填充到节点字段中
- 使用**调试功能**连接设备控制器，实际运行 Pipeline 并实时反馈执行状态

## 调试与任务配置

### 构建任务流程

在画布中构建任务流程。以"领取每日奖励"为例，流程为：识别主页按钮 → 点击进入 → 识别奖励列表 → 逐个领取。

每个节点需配置识别类型和参数，例如使用 TemplateMatch 模板匹配识别按钮、使用 OCR 识别文字内容，动作部分配置点击坐标或区域。

### 调试准备

配置完成后，打开调试面板，依次完成以下准备：

1. **配置资源路径**：指向 Pipeline JSON 所在目录
2. **选择入口节点**：任务的起始点
3. **连接控制器**：选择调试设备（安卓模拟器或真机）

### 运行调试

准备就绪后，点击 **启动调试**。

MPE 会创建调试会话，实时显示当前执行的节点。正在识别的节点会高亮显示，可清晰查看 Pipeline 的运行流。

若某个节点识别失败，调试面板会显示错误信息，可点击查看识别详情（算法绘制图像、匹配得分、ROI 区域等），便于快速定位问题。

执行历史会记录每个节点的执行顺序和耗时，可用于分析性能瓶颈。

调试过程中可随时停止，修改配置后重新启动。

### 配合 VSCode 插件调试

对于更复杂的调试场景，可在 VSCode 中打开项目，使用 MaaFramework 插件启动调试，查看更详细的日志输出，适合深度排查问题。

## 使用 MFAA 本地打包

调试完成后，使用 MaaFramework 提供的 MFAA 打包工具将项目打包为资源包。

在项目根目录运行打包命令，MFAA 会自动收集 resource 目录下的所有配置和资源文件，生成标准化的资源包。

打包前需确保文件结构符合规范：

- Pipeline JSON 放在 `pipeline` 子目录
- 图片模板放在 `template` 目录
- 其他资源分类存放

MPE 的文件管理功能可用于检查和整理文件结构。打包成功后得到的资源文件可直接分发，用户导入 MaaFramework 运行时即可使用。

## 使用 CI 在线打包与发布

通过配置 CI 流程可实现自动化发布。以 GitHub Actions 为例：

1. 在项目仓库中添加工作流配置文件，设置触发条件为推送 TAG 时自动执行
2. 工作流内容：检出代码 → 安装依赖 → 运行 MFAA 打包 → 上传产物到 Release

配置完成后，每次创建新 TAG 并推送，GitHub 会自动执行打包流程，并将资源包附加到 Release 页面。

## 反馈与支持

如在使用过程中遇到问题，可通过以下渠道反馈：

- MaaFramework 交流群：[595990173](https://qm.qq.com/q/gqSv6ukjV8)
- GitHub Issues：<https://github.com/kqcoxn/MaaPipelineEditor/issues>
